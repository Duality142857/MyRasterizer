#include<mygeo/vec.h>
#include"canvas.h"
// MyRasterizer::Vertex


static void genBox(std::vector<MyRasterizer::Vertex>& vertices)
{
    // MyRasterizer::Vertex a,b,c,d;
    // a.pos={0,0,0,1};
    // a.color={255,255,255};

    // b.pos={1,0,0,1};
    // b.color={255,0,0};

    // c.pos={0,1,0,1};
    // c.color={0,255,0};

    // d.pos={0,0,1,1};
    // d.color={0,0,255};

    // vertices.resize(12);
    // vertices={a,c,b,a,d,c,a,b,d,b,c,d};
}
